# 📋 יומן משימות אישי — Task Manager

אפליקציית ניהול משימות אישית בעברית (RTL). קובץ HTML יחיד, ללא שרת וללא התקנה מורכבת.
הנתונים נשמרים מקומית בדפדפן (`localStorage`).

**🔗 גרסה חיה:** https://feldmoni.github.io/Task-Manager/

---

## מה האפליקציה יודעת לעשות

| יכולת | פירוט |
|---|---|
| הוספת משימה | תיאור, קטגוריה (עבודה/בית), תת־קטגוריה, תאריך יעד |
| עריכה | לחיצה על שם המשימה פותחת חלון עריכה מלא |
| סטטוסים | 🔵 פתוחה · 🟡 בתהליך · 🟢 הושלמה · ⚫ בוטלה |
| סימון "דרושה התייעצות" | מסומן בתג סגול ונספר בלוח המונים |
| סינון | פתוחות · היום · עבר זמנן · עבודה · בית · התייעצות · הכל |
| מונים | פתוחות / להיום / עבר זמנן / להתייעצות |
| יצוא Word | טבלת משימות פתוחות כקובץ `.doc` |
| גיבוי ושחזור | ייצוא/ייבוא כל הנתונים כקובץ JSON |

---

## איפה נשמרים הנתונים

הנתונים נשמרים ב־`localStorage` של הדפדפן, תחת המפתח `taskJournal`.

> ⚠️ **חשוב:** לכל מכשיר/דפדפן יש רשימת משימות **נפרדת**.
> המשימות במחשב לא מסתנכרנות אוטומטית עם הטלפון.
> כדי להעביר משימות בין מכשירים: **🗄 גיבוי** במכשיר אחד → **📂 שחזור** בשני.
> מומלץ לעשות גיבוי מדי פעם — ניקוי היסטוריית הדפדפן מוחק את המשימות.

---

## דרכי שימוש

**1. אפליקציית Windows** (מותקנת במחשב)
מותקנת ב־`%LOCALAPPDATA%\PersonalTaskJournal\` ורצה כאפליקציית Edge עצמאית עם אייקון בתפריט התחל.
לעדכון לגרסה האחרונה מהריפו:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\update-windows-app.ps1
```

**2. דפדפן / טלפון**
פשוט פותחים את https://feldmoni.github.io/Task-Manager/ — עובד בכל דפדפן.

**3. מקומי**
פתיחה ישירה של `index.html` בדפדפן.

---

## מבנה הפרויקט

```
index.html                        האפליקציה — הכל בקובץ אחד (HTML + CSS + JS)
dist/Task-Manager-Setup.bat       ⭐ קובץ התקנה יחיד ועצמאי (מוכן להעברה)
installer/                        מקורות ההתקנה
  התקן.bat                        הרצה → מתקין (דורש את התיקייה)
  הסר התקנה.bat                   הרצה → מסיר
  install_journal.ps1             לוגיקת ההתקנה
  icon.ico                        אייקון האפליקציה
  הוראות.txt                      הסבר למשתמש
scripts/build-installer.ps1       בונה את dist/Task-Manager-Setup.bat
scripts/make-icon.ps1             בונה את installer/icon.ico (וי לבן על רקע כחול)
scripts/update-windows-app.ps1    עדכון אפליקציית ה-Windows לגרסה שבריפו
archive/task-manager-simple.html  גרסה מוקדמת ומצומצמת יותר (לא בשימוש)
```

### קובץ התקנה יחיד ⭐

`dist\Task-Manager-Setup.bat` — **קובץ אחד, ~70KB, בלי תלויות ובלי אינטרנט.**
מעתיקים ל-USB או שולחים במייל, לחיצה כפולה על מחשב היעד, וזהו.

| | |
|---|---|
| התקנה רגילה | לחיצה כפולה |
| התקנה שקטה | `Task-Manager-Setup.bat /silent` |

**איך זה עובד:** `index.html`, `icon.ico` ו-`install_journal.ps1` מקודדים ב-Base64
ומוטמעים בסוף קובץ ה-bat. ה-bat מחלץ אותם ל-`%TEMP%`, מריץ את ההתקנה ומנקה אחריו.

> למה Base64: קובץ `.bat` נקרא ע"י cmd.exe בקידוד ה-OEM של המחשב (862 / 437 / 1255,
> תלוי מכונה) — עברית היתה נשברת במחשב אחר. Base64 הוא ASCII טהור ולכן חסין לקידוד.
> מסיבה זו ה-bat עצמו כתוב אנגלית בלבד, וכל העברית יושבת בתוך המטען המוטמע.

**לבנייה מחדש אחרי כל שינוי ב-`index.html`:**

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\build-installer.ps1
```

### התקנה על מחשב חדש

```bash
git clone https://github.com/Feldmoni/Task-Manager.git
```

ואז הרצה של `installer\התקן.bat`. נוצרים שני קיצורים לאותה אפליקציה —
**Task-Manager** ו-**יומן משימות אישי** — כדי שחיפוש בתפריט Start ימצא בשתי השפות.

> ⚠️ קבצי `.ps1` עם טקסט בעברית **חייבים** להישמר כ-UTF-8 **עם BOM**.
> PowerShell 5.1 קורא אותם כ-ANSI בלעדיו והעברית שוברת את הפרסר.

---

## פיתוח

אין שלב build ואין תלויות — עורכים את `index.html` ומרעננים את הדפדפן.

```bash
git clone https://github.com/Feldmoni/Task-Manager.git
cd Task-Manager
```

לפרסום שינויים:

```bash
git add -A && git commit -m "תיאור השינוי" && git push
```

GitHub Pages מתעדכן אוטומטית תוך כדקה.
